const createHandler = () => {};

const createCommand = {
    command: ['create', 'c'],
    description: 'Create a new template',
    builder: () => {},
    handler: createHandler,
};

export default createCommand;
